version: '3'
services:
  front:
    build: ./react-app
    volumes:
    - ./react-app:/opt/react
    - /opt/react/node_modules
    ports:
    - 3000:3000
    links:
    - api

  api:
    build: ./server
    volumes:
    - "./server:/opt/api"
    - /opt/api/node_modules
    ports:
    - 5000:5000
